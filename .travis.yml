language: go
sudo: false
install:
  - export GOROOT=$(go env GOROOT)
  - go get github.com/alecthomas/gometalinter
  - gometalinter --install
  - go get -d -t ./...
  - go install ./...
script:
  - gometalinter --disable errcheck --deadline 10m ./...
  - go test ./...
go:
  - 1.8.x
  - 1.9
  - tip
